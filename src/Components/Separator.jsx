import React from 'react';
import { StyleSheet, View } from 'react-native';

const Separator = () => {
    return (
        <View className="border border-solid border-gray-400 w-10 mx-auto mb-3 my-7" />
    );
}

const styles = StyleSheet.create({})

export default Separator;
